# Frontend environment variables for TeleMed-Pro (CRA)
# Copy this file to frontend/.env and set values

# Backend API base URL
REACT_APP_API_BASE_URL=http://localhost:8000

# Optional: disable hot reload completely (used by craco.config.js)
DISABLE_HOT_RELOAD=false

